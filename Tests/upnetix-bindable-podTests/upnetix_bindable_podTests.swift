import XCTest
@testable import upnetix_bindable_pod

final class upnetix_bindable_podTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(upnetix_bindable_pod().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
